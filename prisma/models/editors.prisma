model Editor {
  editor_id       Int            @id @default(autoincrement())
  editor_name     String
  editor_email    String         @unique
  is_active       Boolean        @default(true)
  editor_password String?        @db.VarChar(500)
  AssignEditor    AssignEditor[]
}
